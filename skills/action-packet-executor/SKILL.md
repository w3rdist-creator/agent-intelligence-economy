---
name: action-packet-executor
description: Turn a report recommendation list into bounded verified actions instead of another plan.
version: 0.1.0
---

# Action Packet Executor


Use when the user says make it so / do 1-7 / execute the recommendations.

Reconstruct the action packet, map each item to a target artifact, perform only scoped changes, verify, record completed/blocked/deferred, and report handles.
