# Sandbox Vault Pattern

Use a sandbox when you want pressure, speed, or exploration without polluting canon.

Main vault = protected truth.
Sandbox vault = wind tunnel / decay chamber / pressure cooker.

Promotion ladder:

```text
pressure-born idea -> reviewed in sandbox -> falsifier check -> promotion packet -> explicit acceptance -> main vault edit
```

Preserve failed and decayed patterns. Null results are evidence.
